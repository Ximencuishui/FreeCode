# FreeCoder 测试计划与用例

**版本**：1.0
**状态**：定稿
**更新日期**：2026-08-19
**适用版本**：FreeCoder 0.1.x


## 一、文档说明

### 1.1 文档目的
本文档定义 FreeCoder 桌面应用的测试策略、测试环境、测试用例及质量验收标准，确保 0.1.x 版本的功能稳定性与用户体验质量。

### 1.2 适用范围
- 单元测试：各模块独立功能验证
- 集成测试：模块间协作与 IPC 通信验证
- 端到端测试（E2E）：完整用户旅程验证
- 性能测试：启动时间、响应速度验证
- 兼容性测试：跨平台验证

### 1.3 测试原则

| 原则 | 说明 |
|------|------|
| **用户视角优先** | 测试用例从非技术用户的操作场景出发，而非技术实现细节 |
| **自动化优先** | 核心回归用例全部自动化，手工测试仅用于探索性测试 |
| **持续集成** | 每次 PR 自动运行单元测试和集成测试 |
| **真实环境** | E2E 测试在真实 Electron 环境中运行，不 mock UI 层 |


## 二、测试环境

### 2.1 硬件与操作系统

| 平台 | 最低配置 | 推荐配置 |
|------|---------|---------|
| **Windows** | Windows 10 1809+, 4GB RAM | Windows 11, 8GB RAM |
| **macOS** | macOS 10.15+, Intel/Apple Silicon | macOS 13+, Apple Silicon |
| **Linux** | Ubuntu 20.04+, 4GB RAM | Ubuntu 22.04, 8GB RAM |

### 2.2 软件依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| Node.js | 18.x / 20.x | 运行环境 |
| pnpm | 8.x | 包管理器 |
| Electron | 28.x | 桌面框架 |
| Playwright | 1.40+ | E2E 测试框架 |
| Jest | 29.x | 单元测试框架 |
| React Testing Library | 14.x | React 组件测试 |

### 2.3 测试工具链

```bash
# 单元测试
pnpm test:unit

# 集成测试
pnpm test:integration

# E2E 测试
pnpm test:e2e

# 测试覆盖率
pnpm test:coverage

# 所有测试
pnpm test
```


## 三、测试策略

### 3.1 测试金字塔

```
          ┌─────────────┐
          │  E2E 测试   │  ← 核心用户旅程（少量）
          │  (5-10)     │
        ┌─┴─────────────┴─┐
        │   集成测试       │  ← 模块协作、IPC（中等）
        │   (20-30)       │
      ┌─┴─────────────────┴─┐
      │     单元测试         │  ← 函数、组件（大量）
      │     (80-100)        │
      └─────────────────────┘
```

### 3.2 测试类型与责任

| 测试类型 | 负责团队 | 触发时机 | 通过标准 |
|---------|---------|---------|---------|
| 单元测试 | 开发 | 每次提交 | 100% 通过 |
| 集成测试 | 开发 | 每次 PR | 100% 通过 |
| E2E 测试 | QA | 每次发版 | 100% 通过 |
| 手工探索测试 | QA | 每周 | 无阻塞性 Bug |
| 性能测试 | 开发 | 里程碑 | 达标 |
| 兼容性测试 | QA | 每个平台发版 | 核心功能可用 |

### 3.3 测试数据管理

- **测试项目**：使用预定义的测试项目（如“测试记账本”），不依赖真实用户数据
- **Mock API Key**：使用测试专用的 DeepSeek API Key，或 Mock 响应
- **隔离存储**：测试使用独立的 `~/.freecoder-test/` 目录，不影响用户数据


## 四、单元测试

### 4.1 测试范围

| 模块 | 测试重点 | 测试框架 |
|------|---------|---------|
| 信号映射与翻译 | 信号匹配、翻译准确性 | Jest |
| 需求结构化 | JSON Schema 验证、字段完整性 | Jest |
| 存储模块 | 读写操作、原子写入、错误处理 | Jest |
| 加密模块 | API Key 加密/解密 | Jest |
| 工具函数 | 格式化、验证、脱敏 | Jest |
| React 组件 | 渲染、交互、状态更新 | React Testing Library |

### 4.2 核心单元测试用例

#### 4.2.1 信号翻译器测试

| 用例 ID | 测试场景 | 输入 | 预期输出 |
|---------|---------|------|---------|
| UT-SIG-001 | 匹配数据库信号 | `"[Agent] 检测到数据库需求"` | `{ message: "您的项目需要保存数据…", category: "question" }` |
| UT-SIG-002 | 匹配错误信号 | `"[Agent] 错误：ECONNREFUSED"` | `{ message: "连接出了点小问题…", category: "error" }` |
| UT-SIG-003 | 未知信号降级 | `"[Agent] 未知技术信息"` | `{ message: "[技术信息] 未知技术信息", category: "info" }` |
| UT-SIG-004 | 信号脱敏 | `"API Key: sk-abc123..."` | `"API Key: [REDACTED]"` |

#### 4.2.2 需求结构化测试

| 用例 ID | 测试场景 | 输入 | 预期输出 |
|---------|---------|------|---------|
| UT-REQ-001 | 完整需求生成 | 完整对话记录 | 包含所有必填字段的 JSON |
| UT-REQ-002 | 部分信息缺失 | 不完整对话 | 缺失字段为 null，不报错 |
| UT-REQ-003 | Schema 验证 | 生成的 JSON | 符合 Requirements Schema |
| UT-REQ-004 | 需求确认 | `confirmRequirements()` | `confirmed: true`, `confirmedAt` 有值 |

#### 4.2.3 存储模块测试

| 用例 ID | 测试场景 | 验证点 |
|---------|---------|--------|
| UT-STO-001 | 创建项目 | 目录结构正确，meta.json 生成 |
| UT-STO-002 | 保存对话消息 | 消息追加，不覆盖已有数据 |
| UT-STO-003 | 原子写入 | 写入过程中断，数据不损坏 |
| UT-STO-004 | 读取不存在的项目 | 返回 null，不抛异常 |
| UT-STO-005 | 对话历史归档 | 超过 1000 条自动归档 |

#### 4.2.4 加密模块测试

| 用例 ID | 测试场景 | 验证点 |
|---------|---------|--------|
| UT-ENC-001 | API Key 加密 | 加密后不等于原文 |
| UT-ENC-002 | API Key 解密 | 解密后等于原文 |
| UT-ENC-003 | 无 Key 时加载 | 返回 null，不抛异常 |
| UT-ENC-004 | 损坏数据解密 | 抛异常，不崩溃 |


## 五、集成测试

### 5.1 测试范围

| 集成点 | 测试重点 |
|--------|---------|
| IPC 通信 | 请求-响应正确性、事件推送 |
| DSH 进程管理 | 启动/停止、stdin/stdout 通信 |
| 信号拦截管道 | DSH 输出 → 拦截 → 翻译 → 推送 |
| 预览服务器 | 启动/停止、文件托管、热加载 |
| 存储与业务层 | 数据持久化正确性 |

### 5.2 核心集成测试用例

#### 5.2.1 IPC 通信测试

| 用例 ID | 测试场景 | 步骤 | 预期结果 |
|---------|---------|------|---------|
| IT-IPC-001 | 发送用户消息 | 调用 `chat:send` | 返回 `success: true`，消息被处理 |
| IT-IPC-002 | 接收 AI 响应 | 发送消息后监听 `chat:response` | 收到 AI 回复事件 |
| IT-IPC-003 | 接收信号推送 | DSH 输出信号 | `chat:signal` 事件触发，内容已翻译 |
| IT-IPC-004 | 预览启动 | 调用 `preview:start` | 返回预览 URL，预览服务器启动 |
| IT-IPC-005 | 项目创建 | 调用 `project:create` | 返回 projectId，目录已创建 |

#### 5.2.2 DSH 进程管理测试

| 用例 ID | 测试场景 | 步骤 | 预期结果 |
|---------|---------|------|---------|
| IT-DSH-001 | DSH 启动 | 调用 `start()` | 进程启动，状态为 `running` |
| IT-DSH-002 | DSH 接收指令 | 通过 stdin 发送指令 | DSH 响应输出到 stdout |
| IT-DSH-003 | DSH 停止 | 调用 `stop()` | 进程终止，状态为 `stopped` |
| IT-DSH-004 | DSH 异常退出 | 模拟 DSH 崩溃 | 管理器检测到退出，可自动重启 |
| IT-DSH-005 | API Key 无效 | 启动 DSH 时 Key 无效 | 返回明确错误，应用不崩溃 |

#### 5.2.3 预览服务器测试

| 用例 ID | 测试场景 | 步骤 | 预期结果 |
|---------|---------|------|---------|
| IT-PRV-001 | 启动预览 | 调用 `preview:start` | 端口可用，返回 URL |
| IT-PRV-002 | 预览内容 | 访问预览 URL | 页面正常加载 |
| IT-PRV-003 | 热加载 | 修改项目文件 | 预览自动刷新 |
| IT-PRV-004 | 端口冲突 | 端口被占用时启动 | 自动尝试下一个端口 |
| IT-PRV-005 | 停止预览 | 调用 `preview:stop` | 服务器关闭，端口释放 |


## 六、端到端（E2E）测试

E2E 测试使用 Playwright 驱动 Electron 应用，模拟真实用户操作。

### 6.1 测试环境配置

```typescript
// e2e/playwright.config.ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  timeout: 60000,
  use: {
    // 使用 Playwright 的 Electron 支持
    // 参考：https://playwright.dev/docs/api/class-electron
  }
});
```

### 6.2 核心 E2E 测试用例

#### 6.2.1 完整用户旅程：从想法到预览

| 用例 ID | E2E-001 |
|---------|---------|
| **测试名称** | 完整用户旅程：创建记账应用 |
| **前置条件** | 已配置有效的 API Key |

**测试步骤**：

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1 | 启动 FreeCoder 应用 | 应用窗口打开，显示对话界面 |
| 2 | 输入“我想做个记账工具” | 消息显示在对话中 |
| 3 | AI 回复引导问题 | 显示选项按钮（个人使用/家庭共用/团队使用） |
| 4 | 点击“个人使用” | 选项被选中，AI 继续引导 |
| 5 | 完成所有引导步骤 | 需求卡片生成并显示 |
| 6 | 点击“确认” | 需求卡片状态变为“已确认”，进度指示开始 |
| 7 | 等待 DSH 完成开发 | 预览窗口自动打开，显示应用界面 |
| 8 | 验证预览内容 | 页面包含标题、输入框、按钮等元素 |

**预期结果**：用户从输入想法到看到可交互预览，全程无需接触代码。

#### 6.2.2 预览交互与修改

| 用例 ID | E2E-002 |
|---------|---------|
| **测试名称** | 预览悬停识别与口语修改 |
| **前置条件** | 项目已生成预览 |

**测试步骤**：

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1 | 在预览窗口中悬停标题 | 标题出现高亮边框，右侧面板显示元素信息 |
| 2 | 右侧面板显示元素属性 | 显示颜色、字号、字重等 |
| 3 | 在输入框输入“标题颜色太深了” | 消息发送成功 |
| 4 | 等待 AI 处理 | AI 回复确认修改意图 |
| 5 | 预览刷新 | 标题颜色发生变化 |
| 6 | AI 询问“您觉得满意吗？” | 显示确认按钮 |

**预期结果**：用户通过悬停和口语完成样式修改，无需接触 CSS 代码。

#### 6.2.3 导出部署包

| 用例 ID | E2E-003 |
|---------|---------|
| **测试名称** | 导出部署包 |
| **前置条件** | 项目已开发完成，处于 `ready` 状态 |

**测试步骤**：

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1 | 点击左侧“导出”按钮 | 导出面板弹出 |
| 2 | 点击“导出部署包” | 显示进度提示 |
| 3 | 等待导出完成 | 收到“导出完成”通知 |
| 4 | 检查导出目录 | `.zip` 文件已生成 |
| 5 | 解压检查内容 | 包含源码、Dockerfile、部署指引 |

**预期结果**：导出包可在任意支持 Docker 的服务器上运行。

#### 6.2.4 “开发团队怎么说”功能

| 用例 ID | E2E-004 |
|---------|---------|
| **测试名称** | 查看 DSH 原始日志 |
| **前置条件** | DSH 正在执行任务 |

**测试步骤**：

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1 | 点击“💡 开发团队怎么说？” | 面板展开 |
| 2 | 查看日志内容 | 显示 DSH 的原始输出（已脱敏） |
| 3 | 点击“收起” | 面板折叠 |

**预期结果**：用户可查看底层开发过程，但默认不干扰主对话。

#### 6.2.5 首次启动配置

| 用例 ID | E2E-005 |
|---------|---------|
| **测试名称** | 首次启动 API Key 配置 |
| **前置条件** | 首次启动，无配置文件 |

**测试步骤**：

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1 | 启动应用 | 显示 API Key 配置页 |
| 2 | 输入无效 API Key | 显示“API Key 无效”提示 |
| 3 | 输入有效 API Key | “开始使用”按钮变为可点击 |
| 4 | 点击“开始使用” | 进入主界面，API Key 已保存 |

**预期结果**：用户完成配置后即可开始使用。


## 七、性能测试

### 7.1 性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|---------|
| 应用冷启动时间 | < 5 秒 | 计时启动到窗口渲染完成 |
| DSH 进程启动 | < 3 秒 | 计时调用 start() 到状态变为 running |
| 预览加载时间 | < 3 秒 | 计时调用 start 到页面加载完成 |
| 修改响应时间 | < 5 秒 | 计时用户输入到预览刷新 |
| 内存占用（空闲） | < 200 MB | 任务管理器监控 |
| 内存占用（运行中） | < 500 MB | 任务管理器监控 |
| 导出包生成 | < 10 秒 | 计时导出完整流程 |

### 7.2 性能测试用例

| 用例 ID | 测试场景 | 测试方法 | 验收标准 |
|---------|---------|---------|---------|
| PT-001 | 应用冷启动 | 自动化计时 | < 5 秒 |
| PT-002 | 连续对话 50 轮 | 内存监控 | 内存增长 < 100 MB |
| PT-003 | 大型项目预览 | 加载大型项目 | 预览加载 < 5 秒 |
| PT-004 | 同时运行 3 个项目 | 资源监控 | 内存 < 800 MB，CPU 正常 |


## 八、兼容性测试

### 8.1 平台兼容性矩阵

| 功能 | Windows 10 | Windows 11 | macOS Intel | macOS Apple | Ubuntu 20.04 | Ubuntu 22.04 |
|------|-----------|-----------|-------------|-------------|--------------|--------------|
| 应用启动 | ✅ 需测试 | ✅ 需测试 | ✅ 需测试 | ✅ 需测试 | ✅ 需测试 | ✅ 需测试 |
| 对话交互 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 预览渲染 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 导出功能 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 字体渲染 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 快捷键 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

### 8.2 分辨率兼容性

| 分辨率 | 测试重点 |
|--------|---------|
| 1920x1080 | 标准桌面，完整布局 |
| 1366x768 | 小屏笔记本，布局自适应 |
| 2560x1440 | 高 DPI，字体缩放正常 |
| 3840x2160 | 4K，缩放比例正确 |


## 九、安全测试

### 9.1 安全测试用例

| 用例 ID | 测试场景 | 测试方法 | 验收标准 |
|---------|---------|---------|---------|
| ST-001 | API Key 存储安全 | 检查存储文件 | 加密存储，不可直接读取 |
| ST-002 | API Key 内存安全 | 内存分析 | 明文 Key 不持久化在内存中 |
| ST-003 | 日志脱敏 | 检查日志文件 | API Key 等敏感信息已脱敏 |
| ST-004 | IPC 权限隔离 | 尝试调用未暴露接口 | 渲染进程无法访问 |
| ST-005 | 预览沙箱隔离 | 尝试执行系统命令 | 预览页面无法访问本地文件系统 |


## 十、测试用例模板

### 10.1 单元测试模板

```typescript
// src/__tests__/example.test.ts
import { describe, expect, it } from '@jest/globals';

describe('模块名称', () => {
  it('应该正确执行功能', () => {
    const result = someFunction(input);
    expect(result).toEqual(expectedOutput);
  });

  it('应该在异常输入时返回错误', () => {
    expect(() => someFunction(null)).toThrow('参数不能为空');
  });
});
```

### 10.2 E2E 测试模板

```typescript
// e2e/example.spec.ts
import { test, expect, _electron as electron } from '@playwright/test';

test.describe('用户旅程名称', () => {
  let app: ElectronApplication;
  let page: Page;

  test.beforeAll(async () => {
    app = await electron.launch({ 
      args: ['dist/main/index.js'] 
    });
    page = await app.firstWindow();
  });

  test.afterAll(async () => {
    await app.close();
  });

  test('核心场景描述', async () => {
    // 1. 操作
    await page.fill('[data-testid="input"]', '我想做个记账工具');
    await page.click('[data-testid="send-btn"]');
    
    // 2. 验证
    await expect(page.locator('[data-testid="ai-response"]'))
      .toContainText('好的');
  });
});
```


## 十一、缺陷管理

### 11.1 缺陷严重等级

| 等级 | 定义 | 响应时间 |
|------|------|---------|
| **P0 - 致命** | 应用崩溃、数据丢失、核心功能不可用 | 立即修复 |
| **P1 - 严重** | 主要功能异常，影响用户体验 | 24 小时内 |
| **P2 - 一般** | 次要功能异常，有临时绕过方案 | 3 天内 |
| **P3 - 轻微** | UI 瑕疵、文案错误 | 下个版本 |

### 11.2 缺陷处理流程

```
发现缺陷 → 提交 Issue（含复现步骤） → 评审 → 分配 → 修复 → 验证 → 关闭
```

### 11.3 缺陷报告模板

```markdown
## 缺陷报告

**标题**：[简短描述]

**严重等级**：P0 / P1 / P2 / P3

**环境**：
- 操作系统：Windows 11
- FreeCoder 版本：0.1.0

**复现步骤**：
1. 打开应用
2. 执行 XXX 操作
3. 观察到 XXX 现象

**预期结果**：应该 XXX

**实际结果**：实际 XXX

**截图/日志**：[附件]
```


## 十二、测试交付物

### 12.1 测试报告内容

| 项目 | 说明 |
|------|------|
| 测试概述 | 测试范围、测试环境、测试周期 |
| 测试执行统计 | 用例总数、通过数、失败数、通过率 |
| 缺陷统计 | 缺陷总数、按等级分布、修复率 |
| 性能测试结果 | 各项性能指标实测值 |
| 兼容性测试结果 | 各平台测试结论 |
| 遗留问题 | 未修复的缺陷及处理计划 |
| 质量结论 | 是否达到发布标准 |

### 12.2 发布标准

| 标准 | 要求 |
|------|------|
| 单元测试通过率 | ≥ 95% |
| 集成测试通过率 | 100% |
| E2E 核心旅程通过率 | 100% |
| P0/P1 缺陷 | 0 个 |
| 性能指标 | 全部达标 |


## 十三、测试执行计划

### 13.1 各阶段测试安排

| 阶段 | 测试类型 | 执行者 | 频率 |
|------|---------|--------|------|
| 开发阶段 | 单元测试 | 开发者 | 每次提交 |
| PR 阶段 | 单元测试 + 集成测试 | CI | 每次 PR |
| 每日构建 | 全部自动化测试 | CI | 每日 |
| 发版前 | 完整测试套件 + 手工探索 | QA | 每个里程碑 |
| 正式发版 | 回归测试 + 兼容性测试 | QA | 每个平台版本 |

### 13.2 CI/CD 集成

```yaml
# .github/workflows/test.yml
name: Test

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm test:unit
      - run: pnpm test:integration
      - run: pnpm test:e2e  # 仅 Linux 环境
```


## 十四、附录

### 附录 A：测试数据示例

**测试项目需求**：
```json
{
  "project_name": "测试记账本",
  "goal": "个人使用的收支记录工具",
  "target_users": "个人使用",
  "core_features": ["记录收支", "分类统计"],
  "visual_style": "简洁"
}
```

**测试用 API Key**：`sk-test-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### 附录 B：测试工具安装

```bash
# 安装依赖
pnpm add -D jest @types/jest ts-jest @testing-library/react @testing-library/jest-dom
pnpm add -D @playwright/test @playwright/experimental-ct-electron

# 初始化 Playwright
npx playwright install

# 运行测试
pnpm test
```


## 十五、版本历史

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| v1.0 | 2026-08-19 | 初始版本，覆盖单元测试、集成测试、E2E 测试、性能测试、兼容性测试、安全测试 |


**文档结束**