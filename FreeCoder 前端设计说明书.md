# FreeCoder 前端设计说明书

**版本**：1.0
**状态**：定稿
**更新日期**：2026-08-19
**适用版本**：FreeCoder 0.1.x


## 一、设计总纲

### 1.1 设计定位

FreeCoder 的前端是一个 **“对话优先”的创作空间**，而非传统的 IDE 或工具软件。用户打开应用时，感觉不到自己在使用一个“开发工具”，而是在与一位懂技术的朋友聊天。

**设计目标**：
- 让用户忘记“我在用软件”，专注于“我在表达想法”
- 让每一次操作都获得即时、正向的反馈
- 让复杂的技术过程完全隐藏在友好的界面背后

### 1.2 核心设计原则

| 原则 | 说明 | 体现 |
|------|------|------|
| **对话优先** | 对话是主要交互方式，辅以可视化卡片 | 主界面 70% 面积留给对话区域 |
| **选择代替输入** | 能用按钮就别让用户打字 | 所有关键决策点提供选项按钮 |
| **渐进式披露** | 只显示当前需要的信息 | 技术细节默认隐藏，点击才展开 |
| **容错设计** | 用户不会犯错，只有不同的选择 | 所有操作都可撤销、可修改 |
| **正向反馈** | 每一步都有成就感和确定感 | 进度指示、状态提示、完成动画 |

### 1.3 设计语言

**关键词**：温暖、友好、清晰、可信赖

- **语气**：就像一位有耐心的朋友在和你说话
- **视觉**：干净、柔和、不刺眼。圆角大、阴影柔和、留白充足
- **反馈**：每一句话都有回应，每一步都有确认


## 二、整体架构

### 2.1 窗口结构

FreeCoder 桌面端采用 **三栏式布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  标题栏（FreeCoder Logo + 项目名称 + 操作按钮）                │
│  ① 创建项目 → ② 方案探讨 → ③ 版本分段 → ④ 生成代码 → ⑤ 预览调整 → ⑥ 导出 │
├──────────┬──────────────────────────────────────┬──────────────┤
│  左侧栏   │         主工作区                    │  右侧面板    │
│  (窄)    │         (宽)                        │  (可收起)    │
│          │                                      │              │
│  ● 对话   │   ┌────────────────────────────┐   │  当前状态    │
│  ● 预览   │   │                            │   │  上下文信息  │
│  ● 导出   │   │      对话流区域             │   │  元素属性    │
│          │   │                            │   │              │
│          │   └────────────────────────────┘   │              │
│          │   ┌────────────────────────────┐   │              │
│          │   │      输入区域               │   │              │
│          │   └────────────────────────────┘   │              │
├──────────┴──────────────────────────────────────┴──────────────┤
│  状态栏（API Key 状态 / DSH 状态 / 网络状态）                   │
└─────────────────────────────────────────────────────────────────┘
```

**主流程步骤条**：标题栏下方常驻一条横向步骤指示器，把完整创作旅程显性化：

```
① 创建项目 → ② 方案探讨 → ③ 版本分段 → ④ 生成代码 → ⑤ 预览调整 → ⑥ 导出
```

- 根据项目阶段自动高亮当前步骤（`draft`=②方案探讨、`planned`=③版本分段、`developing`=④生成代码、`ready`=⑤预览调整、`exported`=⑥导出）
- 已完成步骤显示 ✅，可点击回看（①②③④跳转对话视图、⑤跳转预览、⑥打开导出面板）
- 核心目的：让非技术用户始终知道"我在哪、下一步是什么"，尤其引导他们在写代码前先做**版本分段（MVP）**

### 2.2 导航方式

FreeCoder 的导航非常简单，用户只需在两个主要视图间切换：

| 视图 | 图标 | 说明 |
|------|------|------|
| **对话** | 💬 | 主要工作区，AI 助理在此与用户交互 |
| **预览** | 🔍 | 显示生成的应用预览，用户可交互和修改 |

左侧栏底部有两个次要功能：
- 📦 导出部署包
- ⚙️ 设置


## 三、页面详细设计

### 3.1 首次启动页

用户首次打开 FreeCoder，看到的是极简的 **API Key 配置页**。

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                                                                 │
│                      ✨ FreeCoder                               │
│                                                                 │
│              “把想法变成可用的软件，                            │
│               像跟朋友聊天一样简单”                            │
│                                                                 │
│                                                                 │
│           ┌───────────────────────────────────────┐            │
│           │ 请输入 DeepSeek API Key               │            │
│           │ [──────────────────────────────────]  │            │
│           │                                       │            │
│           │ 💡 如何获取 API Key？                 │            │
│           │                                       │            │
│           │      [开始使用]                       │            │
│           └───────────────────────────────────────┘            │
│                                                                 │
│                                                                 │
│           我们不上传您的 API Key，它只保存在本地                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**设计要点**：
- 画面中央的大卡片，聚焦用户注意力
- “如何获取 API Key” 是可点击的链接，展开简单指引
- “开始使用”按钮在输入 Key 后才可点击（禁用状态为灰色）
- 底部小字提示隐私安全，建立信任

### 3.2 主界面 - 对话视图

这是用户与 AI 助理交互的主战场。

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  ✨ FreeCoder  项目：我的记账本    [预览] [导出] [⚙️]         │
├──────────┬──────────────────────────────────────┬──────────────┤
│  💬 对话  │  ┌────────────────────────────────┐  │  📋 需求卡片 │
│  🔍 预览  │  │  [AI 头像]                     │  │              │
│          │  │  您好！我是您的产品助理。        │  │  项目名称    │
│          │  │  今天想创造什么？                │  │  我的记账本  │
│          │  │                                 │  │              │
│          │  │  [用户头像]                     │  │  目标用户    │
│          │  │  我想做一个记账工具              │  │  个人使用    │
│          │  │                                 │  │              │
│          │  │  [AI 头像]                     │  │  核心功能    │
│          │  │  好的！谁会用这个工具？          │  │  · 记录收支  │
│          │  │                                 │  │  · 分类统计  │
│          │  │  ┌──────┐ ┌──────┐ ┌──────┐  │  │              │
│          │  │  │个人使用│ │家庭共用│ │团队使用│  │  确认状态    │
│          │  │  └──────┘ └──────┘ └──────┘  │  │  ✅ 已确认   │
│          │  │                                 │  │              │
│          │  └────────────────────────────────┘  │              │
│          │  ┌────────────────────────────────┐  │              │
│          │  │ [📎] [🎙️] 输入框... [发送]    │  │              │
│          │  │ 💡 开发团队怎么说？            │  │              │
│          │  └────────────────────────────────┘  │              │
├──────────┴──────────────────────────────────────┴──────────────┤
│  🟢 DeepSeek API 已连接  |  DSH 就绪  |  项目保存在本地        │
└─────────────────────────────────────────────────────────────────┘
```

**对话气泡设计**：

| 类型 | 背景色 | 对齐 | 头像 |
|------|--------|------|------|
| AI 消息 | 浅灰色（#F5F5F7） | 左对齐 | 圆形 AI 图标 |
| 用户消息 | 品牌色（#4A90D9） | 右对齐 | 圆形用户图标 |
| 系统消息 | 透明 | 居中 | 无头像 |

**选项按钮设计**：
- 出现在 AI 消息下方，与 AI 消息左对齐
- 圆角胶囊样式，间距宽松
- 点击后：选项变为“已选”状态（品牌色填充），并自动在对话中插入用户选择结果
- 点击后，该组按钮变灰，不可再选

### 3.3 预览视图

用户点击左侧“预览”按钮，主工作区切换到应用预览。

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  ✨ FreeCoder  项目：我的记账本    [对话] [预览] [导出]        │
├──────────┬──────────────────────────────────────┬──────────────┤
│  💬 对话  │  ┌────────────────────────────────┐  │  🔍 元素信息 │
│  🔍 预览  │  │                                │  │              │
│          │  │        预览区域                  │  │  您正在查看  │
│          │  │      (内置浏览器)               │  │  主标题      │
│          │  │                                │  │              │
│          │  │    ┌─────────────────────┐    │  │  样式：      │
│          │  │    │  我的记账本          │    │  │  颜色：#1A2B3C│
│          │  │    │  收入 +500  支出 -200│    │  │  字号：32px  │
│          │  │    │  本月结余：+300     │    │  │  字重：700   │
│          │  │    └─────────────────────┘    │  │              │
│          │  │                                │  │  操作：      │
│          │  │  [🔄 刷新] [📸 截图]          │  │  [修改颜色]  │
│          │  └────────────────────────────────┘  │  [调整大小]  │
│          │                                     │  [编辑文字]  │
│          │  悬停任意元素查看信息                │              │
├──────────┴──────────────────────────────────────┴──────────────┤
│  提示：悬停/点击页面元素可查看信息，输入框可提交修改指令        │
└─────────────────────────────────────────────────────────────────┘
```

**预览区域的交互反馈**：

| 用户操作 | 视觉反馈 | 右侧面板变化 |
|---------|---------|-------------|
| 鼠标悬停元素 | 元素周围出现浅蓝色高亮边框 | 显示该元素名称和样式信息 |
| 点击元素 | 高亮边框变为品牌色 | 展开详细属性，显示可调整控件 |
| 框选区域 | 半透明蓝色覆盖层 | 显示该区域内包含的元素列表 |

### 3.4 需求卡片（对话流中嵌入）

需求卡片是 AI 助理整理用户需求后生成的**可视化摘要**，嵌入在对话流中。

**卡片样式**：

```
┌─────────────────────────────────────────────────────────────────┐
│  📋 需求概要                                        [编辑]    │
│  ────────────────────────────────────────────────────────────── │
│  项目名称：我的记账本                                         │
│  一句话描述：个人使用的收支记录工具                           │
│  ────────────────────────────────────────────────────────────── │
│  目标用户：个人使用                                           │
│  核心功能：                                                   │
│  · 记录每日收支  ✓                                           │
│  · 按月份分类统计  ✓                                         │
│  · 导出月度报表  ✓                                           │
│  ────────────────────────────────────────────────────────────── │
│  使用场景：每天记录个人消费，月底查看支出分类                  │
│  视觉风格：简洁、清爽、适合个人使用                           │
│  ────────────────────────────────────────────────────────────── │
│  数据需求：收支金额、分类标签、日期时间                       │
│  ────────────────────────────────────────────────────────────── │
│                                        [确认需求，规划版本]    │
└─────────────────────────────────────────────────────────────────┘
```

**交互方式**：
- 点击“修改需求”：卡片中的字段变为可编辑状态
- 点击“确认需求，规划版本”：AI 助理收到信号，项目进入**版本分段阶段**（`planned`），右侧面板随即出现版本分段计划卡片
- 确认后，卡片状态变为“已确认”

### 3.5 版本分段计划卡片（写代码前的 MVP 切分）

非技术用户普遍追求"大而全"，容易忽略最小可用版本（MVP）的重要性。因此，在用户确认需求后、写代码之前，插入一个**版本分段阶段**（`planned`）：AI 助理把已确认的核心功能按版本切分，引导用户先做 V1。

**卡片样式**（右侧面板）：

```
┌─────────────────────────────────────────────────────────────────┐
│  🗺️ 版本分段计划                                  [先做 MVP]   │
│  ────────────────────────────────────────────────────────────── │
│  一口吃不成胖子：先做出最小可用版本（V1），用起来再慢慢完善。    │
│  勾选您想放进 V1 的功能：                                       │
│  ┌─────────────────────────────────────────────┐               │
│  │ ☑ 记录收支                          V1     │               │
│  │ ☐ 分类统计                          以后再做│               │
│  │ ☐ 月度报表                          以后再做│               │
│  └─────────────────────────────────────────────┘               │
│  其余 2 个功能将在后续版本完善，不会丢失。                       │
│  ────────────────────────────────────────────────────────────── │
│                                   [确认计划，开始开发 V1]      │
└─────────────────────────────────────────────────────────────────┘
```

**设计要点**：
- **AI 主动提议**：版本切分由 AI 基于已确认需求自动生成（V1 只保留最核心的功能，其余归入 V2+ 并给出通俗说明），用户无需自行判断
- **勾选可调整**：用户可勾选/取消哪些功能进入 V1，默认接受 AI 提议即可，降低认知负担
- **兜底保证**：AI 生成失败时使用确定性兜底计划（V1=首个核心功能），流程不中断
- **只开发 V1**：点击“确认计划，开始开发 V1”后，开发团队只实现 V1 功能子集，避免一次性堆砌全部功能；后续可在对话中说"开发 V2"逐步完善
- 计划确认后（`developing`），卡片显示"正在开发 V1（最小可用版本）…"

### 3.6 “开发团队怎么说”面板

当用户点击对话输入框下方的“💡 开发团队怎么说？”时，展开一个**可折叠的技术日志面板**。

**样式**：

```
┌─────────────────────────────────────────────────────────────────┐
│  💡 开发团队怎么说？                          [▼ 收起]        │
│  ────────────────────────────────────────────────────────────── │
│  🟢 10:23:15  团队已接收需求，项目经理开始分析                 │
│  🟢 10:23:22  需求分析完成，分配任务                          │
│  🟡 10:23:30  前端开发员正在创建页面布局...                    │
│  🟢 10:23:45  后端开发员正在配置数据库连接                     │
│  🔵 10:23:50  ⚠️ 检测到需要数据库，已自动配置本地 SQLite      │
│  🟢 10:24:00  前端开发员已完成页面布局                        │
│  🟢 10:24:10  所有任务完成，正在构建预览...                    │
│  ────────────────────────────────────────────────────────────── │
│  当前状态：预览已就绪 ✅                                       │
│  共耗时：55 秒                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**状态指示器**：

| 图标 | 含义 |
|------|------|
| 🟢 | 正常执行 |
| 🟡 | 正在执行 |
| 🔵 | 需要注意（但已自动处理） |
| 🔴 | 出现错误（需要用户介入） |
| ✅ | 已完成 |

### 3.7 导出部署包面板

当用户点击左侧栏“📦 导出”按钮时，弹出导出面板。

**样式**：

```
┌─────────────────────────────────────────────────────────────────┐
│  📦 导出部署包                                                │
│  ────────────────────────────────────────────────────────────── │
│                                                                 │
│  您的应用已准备就绪！即将导出完整的部署包。                     │
│                                                                 │
│  ┌─────────────────────────────────────────────┐               │
│  │  📁 导出内容包括：                          │               │
│  │  ✅ 项目源码                                │               │
│  │  ✅ Dockerfile + docker-compose.yml         │               │
│  │  ✅ 数据库初始化脚本                        │               │
│  │  ✅ 中英文双语部署指引                      │               │
│  └─────────────────────────────────────────────┘               │
│                                                                 │
│  💡 导出的部署包可在任何支持 Docker 的服务器上运行              │
│                                                                 │
│  [📥 导出部署包]  [稍后再说]                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.8 设置面板

用户点击右上角 ⚙️ 图标，打开设置面板。

**设置项**：

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| DeepSeek API Key | 密码输入框 | 空 | 首次启动时输入 |
| API Key 状态 | 状态标签 | 未连接 | 显示当前 Key 有效性 |
| 项目存储位置 | 路径选择器 | `~/FreeCoder/Projects` | 可修改 |
| 界面语言 | 下拉选择 | 简体中文 | 暂时只支持中文 |
| 深色模式 | 开关 | 关闭 | 后续版本支持 |
| 数据导出 | 按钮 | - | 导出所有项目数据 |
| 重置所有设置 | 按钮 | - | 清空所有本地数据 |


## 四、交互细节设计

### 4.1 输入区域

输入区域是用户最频繁操作的区域，设计需特别注意。

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  [📎 上传图片] [🎙️ 语音输入] [输入框...] [发送]              │
│  💡 开发团队怎么说？                                           │
└─────────────────────────────────────────────────────────────────┘
```

**交互行为**：

| 行为 | 反馈 |
|------|------|
| 输入框为空 | 发送按钮为灰色，不可点击 |
| 输入框有内容 | 发送按钮变为品牌色，可点击 |
| 用户按 Enter | 发送消息 |
| 用户按 Shift + Enter | 换行 |
| 语音输入中 | 显示“🎙️ 正在听…”，输入框变为语音波形动画 |
| API Key 无效 | 输入框上方显示红色提示：“API Key 无效，请检查设置” |

### 4.2 进度指示

当 DSH 在后台执行任务时（如生成代码），用户需要看到进度。

**进度指示器样式**（嵌入对话流中）：

```
┌─────────────────────────────────────────────────────────────────┐
│  [AI 头像]                                                     │
│  好的！版本分段计划已确认，正在开发 V1（最小可用版本）…        │
│  ┌─────────────────────────────────────────────────────┐        │
│  │  ████████████░░░░░░░░░░  60%                       │        │
│  │  前端开发员正在创建页面布局...                      │        │
│  │  预计剩余时间：1分30秒                             │        │
│  └─────────────────────────────────────────────────────┘        │
│  💡 开发团队怎么说？点击查看详细进度                          │
└─────────────────────────────────────────────────────────────────┘
```

**进度文案（面向非技术用户）**（用于确认版本分段计划后开发 V1 的阶段）：
- 0%：“开发团队正在启动…”
- 20%：“正在分析您的 V1 需求…”
- 40%：“正在设计页面布局…”
- 60%：“正在编写功能代码…”
- 80%：“正在测试和优化…”
- 100%：“✅ 完成！V1 最小可用版本已就绪”

> 注：确认需求后（版本分段阶段）为**计划加载态**，进度指示显示“正在规划版本分段，先做最小可用版本…”，待计划生成后等待用户确认，而非直接进入代码开发。

### 4.3 通知与提示

| 类型 | 样式 | 停留时间 | 示例 |
|------|------|---------|------|
| 成功 | 绿色背景，右侧滑入 | 3 秒 | “✅ 预览已就绪” |
| 警告 | 黄色背景，右侧滑入 | 5 秒 | “⚠️ 网络连接不稳定” |
| 错误 | 红色背景，右侧滑入 | 需用户关闭 | “❌ API Key 无效” |
| 信息 | 蓝色背景，右侧滑入 | 3 秒 | “💡 您可以悬停预览元素查看信息” |

### 4.4 空状态

| 场景 | 显示内容 |
|------|---------|
| 首次启动 | “欢迎来到 FreeCoder！点击下方输入框开始对话” |
| 无项目 | “您还没有创建任何项目，开始一个新想法吧” |
| 版本分段中（计划加载中） | “正在规划版本分段，先做最小可用版本…” |
| 无预览 | “项目生成后，预览窗口会自动出现” |


## 五、视觉规范

### 5.1 品牌色

| 用途 | 色值 | 说明 |
|------|------|------|
| 主品牌色 | `#4A90D9` | 按钮、链接、强调元素 |
| 主品牌色（悬停） | `#3A7BC8` | 交互反馈 |
| 背景色 | `#FFFFFF` | 主背景 |
| 卡片背景 | `#F8F9FA` | 卡片、对话气泡 |
| 文本主色 | `#1A2B3C` | 正文 |
| 文本次要 | `#6B7A8C` | 辅助文字 |
| 边框色 | `#E8ECF0` | 分割线、边框 |
| 成功 | `#34C759` | 成功状态 |
| 警告 | `#FF9500` | 警告状态 |
| 错误 | `#FF3B30` | 错误状态 |

### 5.2 字体

| 用途 | 字体 | 字号 | 字重 |
|------|------|------|------|
| 标题 | -apple-system, BlinkMacSystemFont | 20px | 600 |
| 正文 | -apple-system, BlinkMacSystemFont | 14px | 400 |
| 辅助文字 | -apple-system, BlinkMacSystemFont | 12px | 400 |
| 按钮文字 | -apple-system, BlinkMacSystemFont | 14px | 500 |
| 代码/技术信息 | SF Mono / Menlo / Consolas | 12px | 400 |

### 5.3 圆角与间距

| 元素 | 圆角 |
|------|------|
| 按钮 | 8px |
| 对话气泡 | 12px |
| 卡片 | 12px |
| 输入框 | 8px |
| 预览窗口 | 8px |

| 间距 | 值 |
|------|-----|
| 组件间距 | 16px |
| 内部间距（padding） | 16px |
| 元素间距 | 8px |

### 5.4 图标风格

- 使用 **Feather Icons** 作为图标库（线条风格，简洁友好）
- 图标尺寸：24px（按钮）/ 20px（导航）/ 16px（小元素）
- 颜色继承文本颜色


## 六、响应式与可访问性

### 6.1 窗口适配

| 窗口宽度 | 布局调整 |
|----------|---------|
| ≥ 1024px | 完整三栏布局 |
| 800-1024px | 右侧面板自动收起，点击才展开 |
| < 800px | 对话视图和预览视图切换（类似移动端） |

### 6.2 可访问性

- 支持键盘导航（Tab、Enter、Esc）
- 支持屏幕阅读器（ARIA 标签）
- 字体最小 14px，保证可读性
- 颜色对比度符合 WCAG 2.0 AA 标准


## 七、技术实现建议

### 7.1 前端技术栈

| 技术 | 说明 |
|------|------|
| React 18 | UI 框架 |
| TypeScript | 类型安全 |
| Zustand | 全局状态管理 |
| Tailwind CSS | 样式框架 |
| React Markdown | 渲染富文本消息 |
| Framer Motion | 交互动画 |
| Lucide React | 图标库 |

### 7.2 状态管理

```typescript
// 主要状态
interface FreeCoderState {
  // 对话状态
  messages: Message[];
  isProcessing: boolean;
  
  // 需求状态
  requirements: Requirements | null;
  isConfirmed: boolean;
  
  // 预览状态
  previewUrl: string | null;
  isPreviewReady: boolean;
  selectedElement: ElementInfo | null;
  
  // 应用状态
  currentView: 'chat' | 'preview';
  apiKey: string | null;
  apiKeyStatus: 'valid' | 'invalid' | 'unknown';
  
  // 项目状态
  projectName: string;
  projectPath: string;
}
```

### 7.3 与 DSH/Electron 的通信

```typescript
// 渲染进程 → 主进程
ipcRenderer.send('dsh:start', { projectPath, requirements });
ipcRenderer.send('dsh:modify', { instruction });

// 主进程 → 渲染进程（信号拦截后）
ipcRenderer.on('dsh:signal', (event, signal) => {
  // 信号映射和翻译
  const translated = translateSignal(signal);
  // 更新 UI
});
```


## 八、用户界面文字规范

### 8.1 语气规范

- 使用“您”而非“你”
- 使用“我们”指代 FreeCoder 团队，建立伙伴关系
- 避免“错误”、“失败”等负面词汇 → 改为“遇到一点小状况”、“正在调整”
- 避免“点击这里” → 直接描述动作，如“开始使用”、“确认需求”

### 8.2 常用文案

| 场景 | 文案 |
|------|------|
| 首次问候 | “您好！我是您的产品助理。今天想创造什么？” |
| 需求确认 | “好的！我整理了一份需求概要，您看看对吗？” |
| 版本分段 | “好的！正在规划版本分段，先做出最小可用版本（V1），用起来再慢慢完善。” |
| 开始开发 | “版本分段计划已确认，正在开发 V1（最小可用版本）…” |
| 预览就绪 | “✅ 您的应用已就绪，可以预览了！” |
| 修改确认 | “已将标题颜色调整为天蓝色，您觉得满意吗？” |
| 导出准备 | “您的应用已准备就绪！即将导出完整的部署包。” |
| API Key 无效 | “API Key 似乎无效，请检查设置后重试。” |


## 九、版本历史

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| v1.1 | 2026-08-21 | 新增主流程步骤条（6 步）与「版本分段计划」阶段：需求确认后先做 MVP 切分（planned），确认计划后才开发 V1 |
| v1.0 | 2026-08-19 | 初始版本，覆盖 0.1.x 所有前端设计 |

---

**文档结束**