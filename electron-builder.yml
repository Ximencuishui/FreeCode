appId: com.freecoder.app
productName: FreeCoder
copyright: Copyright © 2026 FreeCoder

directories:
  output: release

files:
  - dist/**/*
  - package.json

# DSH / Node 运行时内嵌（WP-08 集成后启用）
# extraResources:
#   - from: resources/node
#     to: node
#   - from: resources/dsh
#     to: dsh

win:
  target:
    - target: nsis
    - target: portable
  # icon: resources/icons/icon.ico   # 待提供图标（当前使用默认）

nsis:
  oneClick: false
  perMachine: true

mac:
  category: public.app-category.developer-tools
  # icon: resources/icons/icon.icns   # 待提供图标
  target:
    - dmg
    - zip

linux:
  target:
    - AppImage
    - deb
  # icon: resources/icons/icon.png   # 待提供图标
