# FreeCoder 开发计划

**版本**：2.0
**状态**：执行中
**更新日期**：2026-08-21
**适用版本**：FreeCoder 0.1.x

> 本文档基于项目已有的 6 份设计文档组织开发计划，作为团队（或单人开发者）的执行蓝图。
> 依赖文档：产品需求文档 v3.0、技术架构设计文档 v1.0、前端设计说明书 v1.0、API 接口设计文档 v1.0、数据库设计文档（本地存储方案）v1.0、测试计划与用例 v1.0。


## 一、项目方案概要

### 1.1 一句话定位

**FreeCoder 是一个开源的 Electron 桌面应用，让非技术用户通过自然语言对话，从零生成可运行的软件。** 全部代码与数据保存在本地，用户自备 DeepSeek API Key，无任何云端依赖。

### 1.2 三大核心功能（0.1.x 版本）

| 模块 | 核心能力 | 完成标志 |
|------|---------|---------|
| AI 助理（需求分析师） | 对话引导、需求挖掘、信号翻译、结构化输出（需求卡片） | 用户对话 → 生成并确认需求卡片 |
| 即时预览（可视化反馈） | 内置浏览器、元素悬停识别、口语修改 | 需求确认 → 生成代码 → 预览 → 口语改样式即时生效 |
| 自助导出（部署准备） | 导出源码、Docker 配置、中文部署指引 | 一键生成可部署的 .zip 部署包 |

### 1.3 技术栈与架构

- **四层架构**：用户交互层（React 渲染进程）→ 应用控制层（Electron 主进程）→ AI 引擎层（DSH 子进程）→ 基础设施层（本地存储/安全/预览服务器）
- **核心技术栈**：Electron 28+、React 18、TypeScript 5、Zustand、Tailwind CSS、Vite 5、pnpm
- **AI 引擎**：DeepSeek Harness（DSH）以子进程方式本地运行，通过 stdin/stdout 通信；AI 助理采用 Hermes 范式（结构化输出 + 工具调用 + 状态机约束）
- **存储**：文件系统 + JSON（`~/.freecoder/`），原子写入、增量追加、版本迁移
- **接口**：21 个 IPC 通道（对话 4 / 预览 5 / 项目 4 / 导出 2 / 设置 2 / API Key 2 / 应用 2）
- **安全**：API Key 用 Electron `safeStorage` 加密；渲染进程通过 preload 白名单访问主进程；预览 WebView 沙箱隔离；无遥测

### 1.4 文档资产清单（输入依据）

| 文档 | 作用 | 开发时使用方式 |
|------|------|----------------|
| 产品需求文档 v3.0 | 功能与用户体验定义 | 功能验收的依据 |
| 技术架构设计文档 v1.0 | 模块划分、目录结构、进程通信 | 代码结构按此落地 |
| 前端设计说明书 v1.0 | 界面布局、视觉规范、文案 | UI 组件按此实现 |
| API 接口设计文档 v1.0 | 21 个 IPC 通道定义、错误码 | Preload / IPC 层按此实现 |
| 数据库设计文档 v1.0 | 存储路径、JSON Schema、读写规范 | Storage 模块按此实现 |
| 测试计划与用例 v1.0 | 测试策略、用例、验收标准 | 质量验收的依据 |


## 二、开发目标与范围

### 2.1 目标

完成 FreeCoder 0.1.x MVP：**对话 → 开发 → 预览 → 修改 → 导出** 全闭环，可安装运行的 Windows 桌面应用（macOS/Linux 构建配置就绪），并在 GitHub 以 MIT 协议开源。

### 2.2 范围内（0.1.x 必须交付）

- 首次启动 API Key 配置与验证
- AI 助理需求挖掘对话（含选项按钮、跳过、非顺序回答）
- 需求卡片生成、编辑、确认
- DSH 信号拦截与翻译（"开发团队怎么说"可折叠面板）
- 项目代码生成与本地预览（热加载）
- 预览元素悬停识别 + 右侧元素检查器
- 口语修改（颜色/大小/位置/圆角/字重等）+ 撤销确认闭环
- 导出部署包（源码 + Dockerfile + docker-compose + 部署指引）
- 项目列表 / 创建 / 删除 / 读取、对话历史持久化
- 设置面板（API Key、存储路径、语言、深色模式开关）
- 单元 / 集成 / E2E 测试、性能与安全测试达标
- 打包（NSIS/portable）与 GitHub 开源（README、CONTRIBUTING、LICENSE、CI）

### 2.3 范围外（明确不做，0.1.x 之后再说）

- 一键部署、云数据库、付费服务（见 PRD 第五章，需社区反馈触发）
- 语音输入（🎙️ 按钮 UI 可预留，功能不做）
- 自动更新（electron-updater 预留依赖，不实现）
- 小程序 / App 等更多部署目标
- 多语言（界面暂只支持中文，`en-US` 仅保留字段）

### 2.4 非功能目标（验收参考，摘自测试计划）

| 指标 | 目标值 |
|------|--------|
| 应用冷启动 | < 5 秒 |
| DSH 进程启动 | < 3 秒 |
| 预览加载 | < 3 秒 |
| 修改响应 | < 5 秒 |
| 内存占用（空闲/运行） | < 200 MB / < 500 MB |
| 导出包生成 | < 10 秒 |
| 单元/集成/E2E 通过率 | ≥ 95% / 100% / 100% |


## 三、总体开发策略

### 3.1 策略原则

1. **垂直切片优先**：每个里程碑交付"可运行、可演示"的东西，不做一次性大爆炸集成。
2. **先骨架后血肉**：第一周先打通「Electron 启动 → IPC → 存储 → 空对话界面」最小骨架，后续每个功能在骨架上增量生长。
3. **DSH 集成前置验证（Spike）**：DSH 是最大技术风险点，第一阶段必须做最小联调实验（启动子进程、发一条消息、收 stdout），验证可行后再铺开。
4. **接口契约先行**：以 API 接口设计文档的 21 个通道为契约，Preload 类型定义先行，主进程与渲染进程可并行开发。
5. **测试随写随跑**：每个工作包交付时附带对应单元/集成测试，避免最后集中补测试。

### 3.2 里程碑规划

文档 PRD 建议 3-4 个月，以下按 1 名全职开发者（16 周）排期；多人并行时按第七章压缩。

| 里程碑 | 时间 | 核心交付 | 完成标准 |
|--------|------|---------|---------|
| **M0 工程就绪** | 第 1 周 | 脚手架、目录骨架、CI 雏形 | `pnpm dev` 能启动 Electron 窗口并加载 React 页面 |
| **M1 架构验证** | 第 2-3 周 | 主进程骨架 + 21 个 IPC 通道桩 + 存储 + 安全 | DSH 子进程能启动、能收发一条消息（Spike 通过） |
| **M2 AI 助理闭环** | 第 4-6 周 | 对话全链路、需求挖掘、需求卡片 | 对话 → 需求卡片确认 闭环可用（可演示） |
| **M3 预览与修改** | 第 7-10 周 | 预览服务器、WebView、悬停识别、口语修改 | 代码生成 → 预览 → 口语改样式 → 即时刷新 |
| **M4 自助导出** | 第 11 周 | 导出部署包、部署指引 | 一键生成含 Docker 配置的 .zip |
| **M5 质量加固** | 第 12-13 周 | 全量测试、性能与安全测试、缺陷清零 | 达到第二章非功能目标与发布标准 |
| **M6 打包发布** | 第 14-16 周 | 三平台构建配置、安装包验证、GitHub 开源 | 发布 0.1.0，README/CONTRIBUTING/CI 齐全 |
| **M7 运行时增强** | ✅ 已完成 | API Key 多提供商、保存位置、SQLite 后端、OAuth、官网 | 见下方已完成里程碑 |

### 3.3 已完成里程碑（v2.0 更新）

以下功能已在实际开发中完成，超出原始 M0-M6 规划：

| 提交 | 功能 | 对应工作包 |
|------|------|-----------|
| `bd53bd1` | API Key 配置弹窗与多提供商支持、首启引导、Logo/图标与加密降级 | WP-06 增强 |
| `c4fdd48` | 新增保存位置（SaveLocationDialog）功能与单元测试 | WP-04 增强 |
| `42e3ec8` | SQLite 存储 + 分页搜索排序 + OAuth 第三方登录 | 新增 WP-27 |
| `6d22454` | 新增官网宣传页 (website/) 与 Vercel 部署防呆配置 | 新增 WP-28 |
| `e004579` | 官网强化 DSH 智能体引擎信息 | WP-28 增强 |
| `ac2f0a5` | 前端审计修复——锚点偏移、导航溢出、窄屏适配与健壮性 | WP-24 |
| `8964537` | 质量回归、打包构建与开源准备 | WP-24/25/26 |
| `48ec333` | 质量加固与 E2E 基建 | WP-20/21/22/23 |


## 四、任务分解（WBS）

### 阶段 A：工程与基础设施（对应 M0-M1）

| 编号 | 任务 | 主要内容 | 依赖 | 预估 | 验收标准 |
|------|------|---------|------|------|---------|
| WP-01 | 工程脚手架 | pnpm workspace、tsconfig、Vite、electron-builder.yml、ESLint/Prettier/Husky、按架构文档建目录骨架 | - | 2 天 | `pnpm dev` 启动空窗口；`pnpm build` 通过；lint 通过 |
| WP-02 | 主进程入口与窗口 | `main/index.ts`、`window.ts`、`menu.ts`、应用生命周期（退出前停 DSH） | WP-01 | 2 天 | 窗口创建/关闭正常；退出流程回调就位 |
| WP-03 | Preload 与 IPC 契约 | 按 API 文档实现 `preload/index.ts` 全量类型与 21 个通道的桩（主进程 handle 返回占位），`types/electron.d.ts` | WP-02 | 2 天 | 渲染进程能调用全部通道并拿到桩响应；类型检查通过 |
| WP-04 | 存储模块 | `main/storage/`：StorageManager、原子写入工具、项目 CRUD、需求/对话/设置读写、>1000 条归档、迁移框架 | WP-03 | 3 天 | 通过 UT-STO-001~005；原子写入中断不损坏数据 |
| WP-05 | 安全模块 | `main/security/encryption.ts`：safeStorage 加解密、日志脱敏工具 | WP-03 | 1 天 | 通过 UT-ENC-001~004 |
| WP-06 | 设置与 API Key | settings.json 读写与迁移、api-key 保存/验证（`apikey:save/validate`）、首次启动流程（`firstLaunch` 标记） | WP-04, WP-05 | 2 天 | 首次启动显示配置页；Key 验证失败有明确错误码 |

### 阶段 B：DSH 集成与 AI 助理（对应 M1-M2）

| 编号 | 任务 | 主要内容 | 依赖 | 预估 | 验收标准 |
|------|------|---------|------|------|---------|
| WP-07 | DSH 进程管理器 | `main/dsh/manager.ts`：spawn 子进程、stdin/stdout 通信、状态机（running/stopped/error）、自动重启 | WP-03 | 3 天 | 通过 IT-DSH-001~005；崩溃可自动重启 |
| WP-08 | **DSH 联调 Spike** | 用真实 DeepSeek API Key 跑通「启动 DSH → 发送一条需求指令 → 解析结构化输出」，验证 Hermes 模式与信号流 | WP-07 | 3 天 | 输出符合需求 JSON Schema；确定 DSH 接入方式（CLI 参数/配置注入） |
| WP-09 | 信号拦截与翻译器 | `main/dsh/signals.ts` + `translator.ts`：信号规则库、翻译、脱敏、`chat:signal` 推送 | WP-07 | 2 天 | 通过 UT-SIG-001~004、IT-IPC-003 |
| WP-10 | AI 助理对话流 | `chat:send` 全链路：消息入队 → 调 DSH → 流式 `chat:response` 推送 → 持久化；需求结构化输出（JSON Schema 校验）；错误码映射 | WP-08, WP-09, WP-04 | 4 天 | 通过 IT-IPC-001/002、UT-REQ-001~004 |
| WP-11 | 对话 UI | `components/Chat/`：ChatContainer、Message（气泡/选项按钮/进度指示器）、MessageInput（Enter 发送、空态禁用）、"开发团队怎么说"折叠面板 | WP-03 | 4 天 | 对照前端说明书 3.2/4.1/4.2；组件测试通过 |
| WP-12 | 需求卡片 | RequirementCard：展示/编辑/确认，确认后触发 DSH 开发指令，进度文案（0-100%） | WP-10, WP-11 | 3 天 | 对照前端说明书 3.4；确认后进入 developing 状态 |

### 阶段 C：预览与口语修改（对应 M3）

| 编号 | 任务 | 主要内容 | 依赖 | 预估 | 验收标准 |
|------|------|---------|------|------|---------|
| WP-13 | 预览服务器 | `main/preview/server.ts`：本地 HTTP 服务、静态托管项目代码、文件变更热刷新、端口范围 [3000,3010] 自动避让 | WP-04 | 3 天 | 通过 IT-PRV-001~005 |
| WP-14 | 预览视图 UI | PreviewContainer（WebView 集成、沙箱开启）、PreviewToolbar（刷新/截图）、视图切换 | WP-13 | 3 天 | 预览正常加载；WebView 无法访问本地文件系统 |
| WP-15 | 元素悬停识别 | 注入 DOM 脚本捕获悬停元素（tag/selector/样式/位置），高亮边框，`preview:element` 上报 | WP-14 | 3 天 | 对照前端说明书 3.3；悬停/点击有对应反馈 |
| WP-16 | 元素检查器 | 右侧 ElementInspector：友好名称、样式属性展示、常用调整控件（改色/改大小/改文字） | WP-15 | 3 天 | 对照前端说明书 3.3；控件触发修改指令 |
| WP-17 | 口语修改链路 | 口语 → AI 解析（指向元素/属性/动作）→ 生成修改指令 → 改代码文件 → 预览热刷新 → 满意度确认/撤销 | WP-16, WP-10 | 5 天 | 通过 E2E-002；修改响应 < 5 秒 |

### 阶段 D：导出（对应 M4）

| 编号 | 任务 | 主要内容 | 依赖 | 预估 | 验收标准 |
|------|------|---------|------|------|---------|
| WP-18 | 导出部署包 | `export:start` → 复制源码、生成 Dockerfile/docker-compose.yml/.env.example、生成中文部署指引（README + deploy-guide.html）、zip 打包、`export:complete` 推送 | WP-04 | 3 天 | 通过 E2E-003；zip 内结构符合数据库文档 3.6 |
| WP-19 | 导出 UI | ExportPanel（内容清单、进度、结果通知）、项目状态流转（ready → exported） | WP-18, WP-11 | 2 天 | 对照前端说明书 3.6 |

### 阶段 E：测试与质量（对应 M5）

| 编号 | 任务 | 主要内容 | 依赖 | 预估 | 验收标准 |
|------|------|---------|------|------|---------|
| WP-20 | 单元测试补齐 | 按测试计划第四章：信号翻译、需求结构化、存储、加密、工具、React 组件 | 各 WP | 3 天 | 覆盖 UT-SIG/REQ/STO/ENC 全部用例；通过率 ≥ 95% |
| WP-21 | 集成测试 | 按测试计划第五章：IPC、DSH 进程管理、信号管道、预览服务器、存储与业务 | 各 WP | 3 天 | 覆盖 IT-* 全部用例；通过率 100% |
| WP-22 | E2E 测试 | 按测试计划第六章：5 个核心旅程（完整旅程/预览修改/导出/开发团队日志/首次配置） | WP-19 | 4 天 | E2E-001~005 全部通过 |
| WP-23 | 性能与安全测试 | 按测试计划第七、九章：启动时间、内存、修改响应；API Key 加密、日志脱敏、IPC 隔离、WebView 沙箱 | 各 WP | 2 天 | 性能达标；ST-001~005 通过 |
| WP-24 | 缺陷清零与回归 | 修复 P0/P1 缺陷，全量回归，测试报告产出 | WP-20~23 | 2 天 | P0/P1 = 0；达到发布标准（测试计划 12.2） |

### 阶段 F：打包与开源（对应 M6）

| 编号 | 任务 | 主要内容 | 依赖 | 预估 | 验收标准 |
|------|------|---------|------|------|---------|
| WP-25 | 打包构建 | electron-builder 三平台配置验证、资源内嵌（Node/DSH 运行时）、图标、NSIS/portable 安装包 | WP-01 | 3 天 | Windows 安装包可安装运行；macOS/Linux 配置就绪 |
| WP-26 | 开源准备 | LICENSE(MIT)、README、CONTRIBUTING、GitHub Actions（测试 CI + 构建）、Release 0.1.0 | WP-24, WP-25 | 2 天 | CI 绿；发布 0.1.0 tag 与 Release Notes |


## 五、任务依赖与关键路径

```
WP-01 → WP-02 → WP-03 ─┬→ WP-04 → WP-06 ──────────────┐
                       │                               │
                       ├→ WP-05 ───────────────────────┤
                       │                               │
                       └→ WP-07 → WP-08 → WP-09 → WP-10 → WP-12
                                                    │
                                          WP-11（并行于 WP-10 后半段）
                                                    │
                              WP-13 → WP-14 → WP-15 → WP-16 → WP-17
                                                    │
                                              WP-18 → WP-19
                                                    │
                              WP-20 → WP-21 → WP-22 → WP-23 → WP-24
                                                    │
                                              WP-25 → WP-26
```

**关键路径**：WP-01 → WP-02 → WP-03 → WP-07 → WP-08（Spike，风险最高）→ WP-10 → WP-12 → WP-17 → WP-19 → WP-24 → WP-26

**可并行的支线**：WP-04/05/06（存储安全）与 WP-07/08/09（DSH）在 WP-03 之后可并行；WP-11（对话 UI）可与 WP-10 后半段并行；WP-13~16（预览）与 WP-18（导出）在 WP-17 前可部分并行。


## 六、里程碑 → 工作包映射

| 里程碑 | 包含工作包 |
|--------|-----------|
| M0 工程就绪 | WP-01（+ WP-02 起步） |
| M1 架构验证 | WP-02 ~ WP-08 |
| M2 AI 助理闭环 | WP-09 ~ WP-12 |
| M3 预览与修改 | WP-13 ~ WP-17 |
| M4 自助导出 | WP-18 ~ WP-19 |
| M5 质量加固 | WP-20 ~ WP-24 |
| M6 打包发布 | WP-25 ~ WP-26 |


## 七、团队分工建议

| 规模 | 分工 | 说明 |
|------|------|------|
| 1 人（默认） | 全栈顺序执行 | 按关键路径推进；支线任务穿插（如等待 DSH 验证时做存储模块） |
| 2 人 | A：主进程/DSoH/存储（WP-02~10, 13, 18）；B：渲染进程 UI（WP-11, 12, 14~17, 19） | 以 WP-03 的 IPC 契约 + 前端说明书为界并行，UI 用桩数据先行 |
| 3 人 | 以上 + C：测试与 CI（WP-20~24, 26） | C 从 WP-01 起同步搭测试框架，随写随测 |

> 注：DSH 联调（WP-08）需要真实的 DeepSeek API Key，无论几人分工都建议由主进程负责人优先完成。


## 八、环境与工具准备

### 8.1 本机环境（已确认）

| 工具 | 版本 | 要求 | 结论 |
|------|------|------|------|
| Node.js | v24.16.0 | 文档要求 18/20 | ✅ 可用（LTS 之上，注意 electron 兼容性验证） |
| pnpm | 9.14.0 | 文档要求 8+ | ✅ 可用 |
| git | 2.53.0 | - | ✅ 可用 |
| OS | Windows 10 19045 | 最低 Win10 1809+ | ✅ 满足 |
| DSH 源码 | G:\DSH | 集成所需 | ✅ 本地有 checkout，可参考其 CLI/接口实现 |

### 8.2 待准备事项

1. **DSH 运行时**：确认 G:\DSH 的构建产物/CLI 入口；计划将其作为子进程随应用分发（见架构文档 `resources/dsh/`）。
2. **DeepSeek API Key**：开发联调与 E2E 测试需要（测试用 Key 或 Mock 响应方案）。
3. **Electron 依赖下载**：国内网络下 electron 二进制下载可能慢，建议配置镜像（`ELECTRON_MIRROR`）。
4. **版本决策**：Electron 28+ 与 Node 24 的兼容性需在 WP-01 验证；若遇问题可降级 Node 至 20 LTS。

### 8.3 仓库初始化建议

```
freecoder/                    # 新建 git 仓库（或直接使用 E:\FreeCoder 作为仓库根）
├── 6 份设计文档（迁移到 docs/ 或保留根目录）
├── src/ 主进程+渲染进程+preload（按架构文档 4.1 目录）
├── tests/
├── docs/（README/CONTRIBUTING 于 WP-26 补齐）
└── package.json / pnpm-workspace.yaml（WP-01 创建）
```


## 九、风险与应对

| 风险 | 等级 | 影响 | 应对措施 |
|------|------|------|---------|
| **DSH 集成方式不确定**（CLI 接口、子进程通信、Hermes 模式配置与文档不符） | 高 | 阻塞 M1-M2 | WP-08 提前 3 天 Spike；失败则降级为直接调用 DeepSeek API + 自研对话编排，仍保留信号拦截层 |
| DeepSeek API 不可用/限流（联调、E2E 依赖网络） | 中 | 阻塞联调 | Mock 响应层 + 测试 Key；E2E 允许 mock |
| Electron 28 与 Node 24 / pnpm 9 兼容问题 | 中 | 阻塞 WP-01 | 快速验证；必要时降 Node 20 / 调整 electron 版本 |
| 预览 WebView 沙箱与本地 HTTP 服务冲突（CSP、权限） | 中 | 影响预览功能 | 提前验证沙箱配置；元素识别用注入脚本，不开 nodeIntegration |
| 口语修改意图解析不准（非技术用户表述多样） | 中 | 影响核心体验 | 定义有限修改意图集合（颜色/大小/位置/圆角/字重）+ 明确澄清机制（Hermes `ask_clarification`） |
| 打包体积/安装包过大（~120MB，含 Node+DSH） | 低 | 影响分发 | 按需裁剪 DSH 运行时；首版接受体积，文档说明 |
| 单一开发者排期过紧（16 周全栈） | 中 | 延期 | 里程碑内裁剪：M3 先做颜色/文字修改，圆角/字重延后；UI 以桩数据先行 |


## 十、质量与发布标准（摘自测试计划 12.2）

| 标准 | 要求 |
|------|------|
| 单元测试通过率 | ≥ 95% |
| 集成测试通过率 | 100% |
| E2E 核心旅程通过率 | 100% |
| P0/P1 缺陷 | 0 个 |
| 性能指标 | 全部达标（第二章 2.4） |
| 安全审计清单 | 全部勾选（架构文档 7.3） |

**发布门槛**：M6 前完成 WP-24 缺陷清零与回归，产出测试报告（测试计划 12.1 要求的内容）。


## 十一、交付物清单

| 阶段 | 交付物 |
|------|--------|
| 每个工作包 | 对应源码 + 单元/集成测试 + lint 通过 |
| M2 | 可演示的「对话 → 需求卡片确认」闭环 |
| M3 | 可演示的「预览 → 口语修改 → 即时刷新」闭环 |
| M4 | 可下载的部署包 .zip |
| M6 | Windows 安装包、0.1.0 Release、GitHub 仓库（README/CONTRIBUTING/LICENSE/CI）、测试报告 |


## 十二、建议的下一步行动

> **v2.0 更新**：M0-M6 核心功能已基本完成，以下为当前阶段优先事项。

1. **完善文档同步**：确保所有设计文档与实际代码保持一致（本次更新已覆盖主要变更）。
2. **补充 E2E 测试**：为新增功能（SaveLocationDialog、OAuth 登录、分页搜索）补充端到端测试用例。
3. **官网内容迭代**：根据社区反馈持续优化 `website/` 内容与 FAQ。
4. **发布 0.1.0**：完成最终回归测试后发布首个开源版本。
5. **收集社区反馈**：通过 GitHub Issues/Discussions 收集用户需求，规划 0.2.x 版本。

---

**文档结束**
